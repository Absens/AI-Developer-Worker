services:
  worker:
    build: .
    env_file:
      - .env
    environment:
      CODEX_HOME: /codex-home
      REPO_PATH: /workspace/project
    volumes:
      - "${TARGET_REPO_PATH}:/workspace/project"
      - codex-home:/codex-home

volumes:
  codex-home:
